// import { Pipe, PipeTransform } from '@angular/core';
//
// @Pipe({
//     name: 'combo-box-filter'
// })
// // author : hautv
// // -----
// export class ComboBoxPipe implements PipeTransform {
//     transform(dataToSort: any[], columnNameToSort: string, stringToSort: string): any[] {
//         let sortedData: any[];
//         sortedData = [];
//         for (let i = 0; i < dataToSort.length; ++i) {
//             if (dataToSort[i][columnNameToSort].search(stringToSort) > -1) {
//                 sortedData.push(dataToSort[i]);
//             }
//         }
//         return sortedData;
//     }
// }
